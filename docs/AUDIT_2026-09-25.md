# Audit & mise en place — CensGo-ANAREKA-CI (25/09/2026)

Base auditée : `main` @ `3216dcd` (10/09/2026). Branche de correctifs : `claude/audit-setup-2026-09-25`.

## Résumé

| Contrôle | Résultat |
|---|---|
| Installation (`npm ci`, Node 22) | ✅ |
| Tests (`npm test`) | ✅ 367/367 → **370/370** (garde-fou CSP ajouté) |
| Lint (`npm run lint`) | ✅ 0 erreur |
| Build (`npm run build`) + service worker PWA | ✅ |
| Secrets dans l'arborescence actuelle | ✅ aucun (seul `.env.example` suivi, hook pre-commit actif) |
| Fonction Edge `ai-agent` : correctif d'auth déployé | ✅ (vérifie session + rôle `agent`/`admin`) |
| Base Supabase `anarekaci-recensement` | 🔴 **vide** (voir P0) |
| `npm audit` | 🟠 1 critique restante (`maplibre-gl`, correctif = version majeure) |

## P0 — Bloquant

### 1. Base Supabase vide
Le projet `anarekaci-recensement` (`dojbdzponyioicetgziw`, eu-north-1) était en statut `COMING_UP` au moment de l'audit :
0 utilisateur, **aucune table dans `public`** (`census_points`, `user_roles`, `cities`…), pas de schéma `storage`.
Les fonctions Edge `ai-agent` et `sheets-sync` sont pourtant déployées dessus.

Conséquences si c'est bien le projet de production : connexion impossible, et la fonction `ai-agent` renvoie 403 à tout le monde (la table `user_roles` n'existe pas).

À faire :
1. Vérifier dans le dashboard si le projet est en cours de restauration après une pause, et si le secret GitHub `VITE_SUPABASE_URL` pointe bien vers ce projet.
2. S'il reste vide : exécuter `supabase/schema.sql` puis `reset_rls.sql`, et enfin les scripts `add_*.sql` requis (photos, villes, invitations, dangers, tournées, bbox si activé).
3. Relancer l'analyse des conseillers de sécurité Supabase une fois les tables créées.

### 2. Rotation de la clé `anon` exposée dans l'historique Git
Toujours documentée comme en attente dans `SECURITY.md`. Si le projet a été recréé ou vidé, c'est le bon moment pour régénérer la clé et mettre à jour le secret GitHub Actions.

## P1 — Corrigé dans cette branche

| Problème | Preuve | Correctif |
|---|---|---|
| **Aperçu photo cassé** dans le formulaire de recensement : le CSP `img-src` n'autorisait pas `blob:`, alors que `censusFormModal.js` affiche `URL.createObjectURL(blob)` | Vérifié dans Chromium : « Refused to load the image 'blob:…' » | `blob:` ajouté à `img-src` + test `src/__tests__/csp.test.js` |
| Bouton « Créer un compte agent » **quasi invisible en mode sombre** (texte `#1a3d2b` sur fond `#152232`, contraste 1,3:1) | Capture mobile avant/après | Utilise `var(--green-dark)` (10,7:1 en sombre, inchangé en clair) ; idem pour le focus du champ de connexion et le survol des boutons de l'écran Agents |
| Texte secondaire en mode sombre sous le seuil WCAG AA (`--text-muted` `#64748b`, contraste 3,4:1) | Calcul du ratio de contraste | `#8494a9` (5,2:1) |
| Bouton « Réessayer » de l'écran d'erreur : texte blanc sur `#2ecc71` (2:1) et hauteur de 40 px | Audit mobile | Texte vert foncé et `min-height: 48px` |
| Vulnérabilités `fast-uri` et `js-yaml` (dépendances de dev, gravité haute) | `npm audit` | `npm audit fix` (lockfile uniquement) |

## P2 — À planifier

- **`maplibre-gl` 4.7 → 6.x** (GHSA-jrc7-96c5-q579, contournement de `DOM.sanitize()`). Le risque réel est faible : les deux popups qui utilisent `setHTML` (`hazards.js`, `agentTracking.js`) échappent déjà toutes les données. Mais la montée de version est majeure (4 → 6) et doit être validée sur un appareil réel (carte, clusters, 3D, tuiles hors-ligne). Le bundle `maplibregl` pèse 780 Ko, c'est aussi le moment d'en mesurer l'impact.
- **La vague P0 « identité visuelle » du 31/08 n'est pas sur `main`** : on trouve encore l'emoji 🗺️ comme logo (favicon, `.login-badge` dans `appShell.js`, `.brand-mark` dans `appView.js`, `public/icon-*.svg`). Ce travail (logo vectoriel pin or/vert forêt, icônes SVG) n'a jamais été poussé ; il est à refaire ou à récupérer.
- **`appView.js` fait 2 873 lignes**, soit environ 20 % du code : c'est le principal point de dette technique. Pistes de découpage : administration (villes, invitations, agents), rapports, tournée, IA.
- Styles inline dans les gabarits HTML de `appView.js` (tournée, stats, aperçu d'image) : ils ignorent le mode sombre, à migrer vers des classes CSS.
- Le toast « CensGo est prêt à fonctionner hors connexion » s'affiche aussi par-dessus l'écran « Impossible de démarrer », ce qui est contradictoire. Il est aussi trop étroit sur mobile (4 lignes).
- `console.log` de performance laissés dans `dataLoader.js` et `main.js` : à faire passer par `core/debug.js`.
- Dépendances à jour de version mineure possibles : `@supabase/supabase-js` 2.117, `vite` 8.3, `dexie` 4.4.6. Pas de couverture de code (`@vitest/coverage-v8` absent).
- Branches distantes obsolètes à supprimer : `claude/audit-setup-fwfdc8`, `claude/audit-setup-6o85fz`, `fix/census-points-navigation-instrumentation-…` (déjà constatées comme sans valeur lors des audits d'août).

## Points solides

- Échappement HTML systématique (`escapeHtml`) sur toutes les données utilisateur injectées dans le DOM.
- CSP stricte (`script-src 'self'`, sans `unsafe-inline` ni `unsafe-eval`), et le bouton « Réessayer » n'utilise plus de `onclick` inline.
- Photos dans un bucket privé, consultées uniquement par URL signée de 5 minutes.
- Fonction Edge IA : contrôle d'origine, session et rôle, et limites de taille sur le texte, les points et les images.
- CI GitHub Actions : actions épinglées par SHA, lint, tests puis build avant le déploiement Pages.
- Bonnes bases d'UX terrain : `lang="fr"`, cibles tactiles ≥ 44 px, champs tous étiquetés, `safe-area`, `reduced-motion`.
